Prompts usados en lista_numeros.py

Escribe un programa en python que pida una lista de números separados por comas, convierta a floats, calcule suma, media y máximo, y detecte duplicados

explícame para que sirven los siguientes trozos de código: float(x.strip()),in entrada.split(","),set([x for x in numeros if numeros.count(x) > 1]),{sorted(duplicados)}

además añade al código que muestre el cuadrado de los números introducidos, muestra los resultados de una forma más vistosa 

explícame este código: zip(numeros, cuadrados): 
    print(f"   {n:>8}  →  {c:.2f}")

Prompts usados en temperaturas.py

Genera un programa en python que sea un conversor de temperaturas: de grados centigrados a kellvin o farenheit

Cambia el código para que el usuario pueda elegir que tipo de unidad quiere ingresar la temperatura y a que tipo convertirla

Prompts usados en cadenas.py

Crea un programa en python donde reciba una cadena y cuente el número de vocales, consonantes, mayúsculas y número de caracteres

explicame esta parte del código: for c in texto:
    if c.isalpha():  # Solo letras
        if c in vocales:
            num_vocales += 1
        else:
            num_consonantes += 1
    if c.isupper():
        num_mayusculas += 1

Agrega en el código de cadenas manejo de errores por si el usuario escribe un número y añade que el programa cuente las minúsculas y las palabras escritas

Prompts usados en primo.py

crea un programa en python que diga si un número introducido por el usaurio es primo o no, que maneje la excepción de el usuario escribe otra cosa que no sea un número entero

explicame paso a paso como funciona esta parte del código: for i in range(2, int(num**0.5) + 1)

añade al código que muestre por pantalla paso a paso la parte de comprobar si un número es primo
